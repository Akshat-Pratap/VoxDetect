# VoxDetect Backend — Runtime Dependencies
# Install inside backend/.venv (never globally):
#   pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Data validation ────────────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# ── HTTP client (webhooks) ─────────────────────────────────────────────────────
httpx>=0.27.0

# ── File uploads ───────────────────────────────────────────────────────────────
python-multipart>=0.0.9

# ── WebSocket support (included in uvicorn[standard] via websockets) ───────────
websockets>=12.0

# NOTE: ML Core dependencies (torch, torchaudio, transformers, librosa,
# soundfile, resemblyzer) are NOT listed here.
# They are managed by P1 in ml-core/requirements.txt.
# Install them separately in the same .venv if you want real ML inference:
#
#   pip install -r ../ml-core/requirements.txt
#
# The backend will start successfully without them — analyze-call will return
# 503 until the ML Core is available.
