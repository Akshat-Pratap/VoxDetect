# VoxDetect Backend — Development Dependencies
# Install after requirements.txt:
#   pip install -r requirements-dev.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0        # also used by webhook service; listed here for test client

# Optional: WebSocket testing support
# httpx-ws>=0.6.0    # uncomment if you want to run WebSocket integration tests
